# Enable automatic trigger creation for all tables in 'public' schema
auto.sync.triggers=true
auto.sync.triggers.mode=all   # can be 'new' to only add new tables

# Sync table structure changes
sync.table.structure=true
sync.triggers.ddl.sql=true

# Ensure all new tables in public schema are included
auto.config.database=true
